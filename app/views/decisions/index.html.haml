%h1 Listing decisions

%table
  %tr
    %th Player
    %th Food
    %th
    %th
    %th

  - @decisions.each do |decision|
    %tr
      %td= decision.player
      %td= decision.food
      %td= link_to 'Show', decision
      %td= link_to 'Edit', edit_decision_path(decision)
      %td= link_to 'Destroy', decision, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Decision', new_decision_path
